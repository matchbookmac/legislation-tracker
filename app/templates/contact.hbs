<ul class="list-unstyled">
  {{#each legislator in model}}
    <li>
          <span class="legislator-name">{{legislator.first_name}} {{legislator.last_name}}</span>
      <br>Contact:
      <a href="{{legislator.contact_form}}">{{legislator.contact_form}}</a>
    </li>
    <br>
  {{/each}}
</ul>
